{
  "name": "cost-manager-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Root test runner for the Cost Manager microservices project",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
